---
title: Cache Penetration, Breakdown, and Avalanche - Quick Guide | 快速指南：緩存穿透、擊穿、雪崩
date: 2026-02-15
tags: Cache, Redis, Distributed Systems, 緩存, 分佈式
---

# Cache Penetration, Breakdown & Avalanche: Complete Guide 緩存穿透、擊穿與雪崩：完整指南

## Key Terms

<Table
  caption="Cache patterns & mitigations · 緩存問題與緩解"
  columns={[
    { key: "term", header: "Term" },
    { key: "meaning", header: "Meaning" },
  ]}
  data={[
    {
      term: "Cache Penetration · 緩存穿透",
      meaning:
        "Requests for non-existent data always go to the DB, bypassing the cache. 當請求不存在的數據時（Cache和DB皆無），會直達DB，讓Cache失去效用。",
    },
    {
      term: "Cache Breakdown · 緩存擊穿",
      meaning:
        'A "hot key" expires: many requests for the same key all miss cache and hit DB. 熱門Key突然過期，所有請求瞬間查不到Cache，全都衝向DB。',
    },
    {
      term: "Cache Avalanche · 緩存雪崩",
      meaning:
        "Many keys expire together, flooding the DB. 大量Key同時失效或系統重啟，瞬間產生大量請求，DB承受不住。",
    },
    {
      term: "Bloom Filter · 布隆過濾器",
      meaning:
        "Efficient way to check if a key definitely does not exist, blocking unnecessary requests. 先檢查Key是否一定不存在，若確定無此Key，就不用再查Cache和DB。",
    },
    {
      term: "Mutex Lock · 互斥鎖",
      meaning:
        "Prevents several requests from rebuilding the same cache at once. 防止多個請求同時重建同一份Cache，透過鎖只允許一個進行，其餘需等待。",
    },
    {
      term: "TTL (Time To Live) · 生存時間",
      meaning:
        "Cache expiration time; use random expiry to avoid avalanche. Cache的過期時間（TTL）；可隨機設置TTL避免集體過期造成雪崩現象。",
    },
    {
      term: "Rate Limiting · 限流",
      meaning:
        "Limit request rate to protect DB. 控制請求速率，避免短時間內大量查詢導致DB崩潰。",
    },
    {
      term: "Fallback / Degrade · 降級/兜底",
      meaning:
        "Graceful fallback when system busy. 系統超載時優雅退回，返回預設值、靜態頁等保底資料，避免癱瘓。",
    },
  ]}
/>

## Cache Penetration 緩存穿透

### Phenomenon · 現象

<BilingualParagraph
  en="Users request a key that does not exist in the DB or cache, so every request misses and hits the database repeatedly."
  zh="用戶請求不存在（DB與Cache皆無）的 Key，怎麼查都查不到，導致 DB 不斷被查詢。"
/>

### Common solutions · 常見做法

<BilingualSolutionList
  items={[
    {
      enLabel: "Cache null value",
      en: "Store empty/null in cache for a short TTL so the next lookup short-circuits and does not hit the DB again.",
      zhLabel: "緩存空值",
      zh: "可設置短暫過期的空值，避免同一 Key 重複擊中 DB。",
    },
    {
      enLabel: "Bloom filter",
      en: "Check whether a key can possibly exist before querying cache or DB; reject impossible keys early.",
      zhLabel: "布隆過濾器",
      zh: "先用過濾器判斷 Key 可能不存在，若無則直接拒絕，不進一步查 DB。",
    },
  ]}
/>

#### Typical flow · 典型流程

<Table
  caption="Typical request flow · 典型請求流程"
  columns={[
    { key: "step", header: "#" },
    { key: "action", header: "Action · 步驟" },
  ]}
  data={[
    {
      step: "1",
      action: "Check cache; if hit, return.（查 Cache，有則直接返回）",
    },
    {
      step: "2",
      action: "Check Bloom filter; if the key cannot exist, return.（查過濾器，無此 Key 則快速返回）",
    },
    {
      step: "3",
      action: "Query DB if needed; cache a null/empty marker if not found.（如有需要再查 DB，若查無則寫入空值於 Cache）",
    },
  ]}
/>

## Cache Breakdown 緩存擊穿

### Phenomenon · 現象

<BilingualParagraph
  en="A hot key expires: many concurrent requests miss cache at once and all hit the database together."
  zh="熱門 Key 突然過期，導致大量請求同時查不到 Cache，全部轉向 DB，造成壓力激增。"
/>

### Prevention methods · 預防方法

<BilingualSolutionList
  items={[
    {
      enLabel: "Mutex lock",
      en: "Only one request rebuilds the cache for that key; others wait or read stale data.",
      zhLabel: "互斥鎖",
      zh: "第一個請求負責重建 Cache，其餘需等待或使用舊數據。",
    },
    {
      enLabel: "Cache prewarm / refresh",
      en: "Refresh or extend the hot key before it expires, or schedule background refresh.",
      zhLabel: "預熱／定時刷新",
      zh: "熱門 Key 到期前主動更新，或指定週期做 refresh。",
    },
    {
      enLabel: "Double delete / never expire",
      en: "Advanced patterns; combine with the strategies above as needed.",
      zhLabel: "雙重刪除／永不過期",
      zh: "高階做法，可配合其他方法一起使用。",
    },
  ]}
/>

## Cache Avalanche 緩存雪崩

### Phenomenon · 現象

<BilingualParagraph
  en="Many keys expire together or the system restarts, so the cache empties and traffic floods the database at once."
  zh="一大批 Key 同時過期或系統重啟，Cache 全失效，導致請求瞬間全部灌向 DB，DB 不堪重負。"
/>

### Common solutions · 常見做法

<BilingualSolutionList
  items={[
    {
      enLabel: "Random TTL",
      en: "When writing cache, use base TTL plus jitter so keys do not expire at the same instant.",
      zhLabel: "隨機過期時間",
      zh: "每個 Key 的過期時間都不一樣，可防止同時過期。",
    },
    {
      enLabel: "Batch cache warmup",
      en: "Preheat or refresh in batches; avoid wiping the entire cache in one shot.",
      zhLabel: "分批預熱 Cache",
      zh: "預熱時分批進行，絕不可一次性清除全部 Cache。",
    },
    {
      enLabel: "Rate limit & fallback",
      en: "On traffic spikes, degrade gracefully and return safe defaults instead of hammering the DB.",
      zhLabel: "限流與降級",
      zh: "流量異常時即時啟動限流，必要時返回預設或兜底資料，避免 DB 被擊穿。",
    },
  ]}
/>
